$('#query_parent_station_id1').
  html("<%= escape_javascript(options_for_select(
    @parent_stations1.collect { |stop| [stop.stop_name, stop.stop_id] }.sort_by { |arr| arr[1] }
  )) %>"
);

$('#query_stop_id1').html("");